services:
  - type: web
    name: home-rhythm-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn fastapi_server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/v1/health
